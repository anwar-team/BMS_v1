# Composer Package Requirements

Add these to your composer.json:

```json
{
    "require": {
        "elasticsearch/elasticsearch": "^7.17",
        "laravel/scout": "^10.0"
    }
}
```

Then run:
```bash
composer install
```