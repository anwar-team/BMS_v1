# 📚 أنماط استخراج قسم الكتاب (Book Section Extraction Patterns)

## 🎯 الهدف
استخراج قسم الكتاب من حقل `description` وربطه بجدول `book_sections`.

---

## 📋 الأنماط الشائعة في الأوصاف

### نمط 1: "القسم:" أو "التصنيف:"
```
القسم: الفقه الإسلامي
التصنيف: العقيدة
الموضوع: الحديث النبوي
```

### نمط 2: ضمن معلومات الكتاب
```
الكتاب: أحكام القرآن
المؤلف: أحمد بن علي
القسم: التفسير وعلوم القرآن
```

### نمط 3: في نهاية الوصف
```
[... معلومات الكتاب ...]
القسم: الفقه > فقه حنفي
```

---

## 🔍 Regular Expressions للاستخراج

### النمط الأساسي
```regex
(?:القسم|التصنيف|الموضوع|المجال)\s*[:：]\s*([^\n\r]+)
```

### أمثلة التطبيق

#### مثال 1
```
النص: "القسم: الفقه الإسلامي"
المستخرج: "الفقه الإسلامي"
```

#### مثال 2
```
النص: "التصنيف: العقيدة والملل والنحل"
المستخرج: "العقيدة والملل والنحل"
```

#### مثال 3
```
النص: "الموضوع: الحديث النبوي - السنة النبوية"
المستخرج: "الحديث النبوي - السنة النبوية"
```

---

## 📊 الأقسام الموجودة في book_sections

دعنا نفحص الأقسام الموجودة حالياً:

```sql
SELECT id, name_ar, parent_id, COUNT(*) as books_count
FROM book_sections bs
LEFT JOIN books b ON b.book_section_id = bs.id
GROUP BY bs.id
ORDER BY books_count DESC;
```

### الأقسام الرئيسية المتوقعة:
1. **الفقه الإسلامي**
   - فقه حنفي
   - فقه مالكي
   - فقه شافعي
   - فقه حنبلي

2. **العقيدة**
   - أصول الدين
   - الملل والنحل

3. **الحديث**
   - الحديث النبوي
   - علوم الحديث
   - مصطلح الحديث

4. **التفسير**
   - التفسير وعلوم القرآن
   - أسباب النزول

5. **اللغة**
   - النحو والصرف
   - البلاغة
   - المعاجم

6. **التاريخ**
   - السيرة النبوية
   - تاريخ الإسلام
   - التراجم

7. **الأدب**
   - الشعر
   - النثر

---

## 🔧 خوارزمية المطابقة

### المرحلة 1: الاستخراج
```php
function extractSection($description) {
    $patterns = [
        '/(?:القسم|التصنيف|الموضوع|المجال)\s*[:：]\s*([^\n\r،]+)/u',
        '/القسم\s*[:：]?\s*([^\n\r،]+)/u',
    ];
    
    foreach ($patterns as $pattern) {
        if (preg_match($pattern, $description, $matches)) {
            return trim($matches[1]);
        }
    }
    
    return null;
}
```

### المرحلة 2: المطابقة
```php
function matchSection($extractedSection) {
    // 1. مطابقة تامة
    $exact = BookSection::where('name_ar', $extractedSection)->first();
    if ($exact) {
        return ['section_id' => $exact->id, 'confidence' => 1.00];
    }
    
    // 2. مطابقة جزئية - يحتوي على
    $partial = BookSection::where('name_ar', 'LIKE', "%{$extractedSection}%")->first();
    if ($partial) {
        return ['section_id' => $partial->id, 'confidence' => 0.85];
    }
    
    // 3. مطابقة عكسية - القسم يحتوي على النص المستخرج
    $sections = BookSection::all();
    foreach ($sections as $section) {
        if (stripos($extractedSection, $section->name_ar) !== false) {
            return ['section_id' => $section->id, 'confidence' => 0.75];
        }
    }
    
    // 4. مطابقة تقريبية باستخدام similar_text
    $bestMatch = null;
    $bestScore = 0;
    
    foreach ($sections as $section) {
        similar_text($extractedSection, $section->name_ar, $score);
        if ($score > $bestScore && $score > 60) {
            $bestScore = $score;
            $bestMatch = $section;
        }
    }
    
    if ($bestMatch) {
        return [
            'section_id' => $bestMatch->id, 
            'confidence' => $bestScore / 100
        ];
    }
    
    // 5. لم يتم العثور - إنشاء قسم جديد
    return null;
}
```

### المرحلة 3: إنشاء قسم جديد (إن لزم)
```php
function createNewSection($sectionName, $parentId = null) {
    return BookSection::create([
        'name_ar' => $sectionName,
        'name_en' => $sectionName, // يمكن ترجمته لاحقاً
        'parent_id' => $parentId,
        'description' => 'قسم تم إنشاؤه تلقائياً من الاستخراج',
        'is_active' => true,
    ]);
}
```

---

## 📊 أمثلة واقعية

### مثال 1: كتاب فقهي
```
الوصف:
بطاقة الكتاب و
الكتاب: أحكام القرآن للجصاص
المؤلف: أحمد بن علي أبو بكر الرازي الجصاص الحنفي
القسم: الفقه الإسلامي - المذهب الحنفي
الناشر: دار إحياء التراث العربي

المستخرج: "الفقه الإسلامي - المذهب الحنفي"
المطابقة: 
  - الخيار 1: "الفقه الإسلامي" (85% ثقة)
  - الخيار 2: "فقه حنفي" (90% ثقة) ✅ الأفضل
```

### مثال 2: كتاب تفسير
```
الوصف:
الكتاب: تفسير الطبري
المؤلف: محمد بن جرير الطبري
التصنيف: التفسير وعلوم القرآن
الناشر: دار المعارف

المستخرج: "التفسير وعلوم القرآن"
المطابقة:
  - مطابقة تامة: "التفسير وعلوم القرآن" (100% ثقة) ✅
```

### مثال 3: كتاب حديث
```
الوصف:
الكتاب: صحيح البخاري
الموضوع: الحديث النبوي الشريف

المستخرج: "الحديث النبوي الشريف"
المطابقة:
  - مطابقة جزئية: "الحديث النبوي" (85% ثقة) ✅
```

---

## ⚠️ حالات خاصة

### 1. قسم فرعي (Sub-section)
```
القسم: الفقه > فقه حنفي
```
**الحل:**
- استخراج "فقه حنفي" كالقسم الفرعي
- البحث عن parent_id = قسم "الفقه الإسلامي"

### 2. أقسام متعددة
```
القسم: الفقه والعقيدة
```
**الحل:**
- تقسيم بالـ "و" أو "،"
- اختيار القسم الأول كأساس
- حفظ "الفقه والعقيدة" كنص مستخرج

### 3. لا يوجد قسم في الوصف
```
بطاقة الكتاب و
الكتاب: كتاب قواعد الإملاء
المؤلف: عبد السلام هارون
```
**الحل:**
- محاولة الاستنتاج من عنوان الكتاب
- استخدام AI/NLP للتصنيف
- تعليم needs_review = true

---

## 🎯 معايير الثقة (Confidence Scores)

| نوع المطابقة | نسبة الثقة | القرار |
|---------------|------------|---------|
| مطابقة تامة 100% | 1.00 | تطبيق تلقائي ✅ |
| مطابقة جزئية (contains) | 0.80-0.90 | تطبيق تلقائي ✅ |
| مطابقة تقريبية (similar_text > 80%) | 0.80-0.90 | تطبيق تلقائي ✅ |
| مطابقة تقريبية (60-80%) | 0.60-0.79 | يحتاج مراجعة ⚠️ |
| مطابقة ضعيفة (< 60%) | 0.00-0.59 | يحتاج مراجعة يدوية ❌ |
| لم يُعثر على قسم | null | إنشاء جديد أو مراجعة ❌ |

---

## 📝 ملاحظات التطبيق

### 1. الأولوية
- **القسم هو الأولوية الأولى** قبل المؤلف والناشر
- يجب استخراجه أولاً لأنه يساعد في تصنيف الكتاب

### 2. التطبيق
```php
// عند التطبيق التلقائي (confidence >= 0.80)
if ($metadata->section_match_confidence >= 0.80) {
    $book->book_section_id = $metadata->matched_section_id;
    $book->save();
    
    $metadata->is_applied = true;
    $metadata->applied_at = now();
    $metadata->save();
}
```

### 3. المراجعة
- الأقسام الجديدة تحتاج موافقة admin
- يمكن دمج الأقسام المتشابهة لاحقاً
- واجهة Filament للمراجعة والموافقة

---

## 🚀 الخطوات التالية

1. ✅ إضافة حقول القسم في Migration
2. ✅ تحديث Model
3. ⏳ إنشاء SectionMatcher Service
4. ⏳ اختبار على 10 كتب
5. ⏳ مراجعة النتائج وتحسين الأنماط

---

**تاريخ الإنشاء:** 5 أكتوبر 2025  
**الحالة:** جاهز للتنفيذ ✅
